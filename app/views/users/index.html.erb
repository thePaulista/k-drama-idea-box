<table class="table">
<h1>All Users</h1>
<thead>
<% @users.each do |user| %>
  <tr>
    <th><%= link_to user.username, user_path(user) %></th>
    <th>  <%= user.email %></th>
  </tr>
<% end %>
</thead>
</table>
